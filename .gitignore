result
result-*
.direnv/
.DS_Store

# matugen / build scratch artifacts
*.tmp

# home-manager's claude-code module owns this under ~/.claude/skills; it must
# never be tracked in the repo (it's regenerated per-rebuild and pointed store)
users/kyandesutter/claude/skills/claude-code-home-manager/
