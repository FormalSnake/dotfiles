# Generated by Noctalia. These colours are pushed to Hyprland live via this
# template's post_hook (`hyprctl eval 'hl.config{…}'`) — see mixins/noctalia.nix.
# Mirrors the exact properties Noctalia's built-in `hyprland` template sets, but
# applied via eval (works with the 0.55 Lua parser; the builtin appends
# require("noctalia") to a read-only HM symlink and doesn't re-apply live). This
# file is just a record of the current values for debugging.
active_border          = {{ colors.primary.default.hex_stripped }}
inactive_border        = {{ colors.surface.default.hex_stripped }}
group_border_active    = {{ colors.secondary.default.hex_stripped }}
group_border_locked    = {{ colors.error.default.hex_stripped }}
group_border_inactive  = {{ colors.surface.default.hex_stripped }}
